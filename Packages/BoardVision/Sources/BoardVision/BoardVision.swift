/// Placeholder for Phase 2 (Board Awareness / Vision).
///
/// This package is intentionally empty. It exists only so the package graph
/// and CI are ready before ScreenCaptureKit/adb capture and CV/OCR land —
/// see README.md's phase table and docs/adr/0001-architecture.md.
public enum BoardVision {
    public static let targetPhase = 2
}
